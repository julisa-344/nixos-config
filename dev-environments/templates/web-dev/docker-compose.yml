# Example docker-compose.yml for development services
# Uncomment and customize the services you need

version: '3.8'

services:
  # PostgreSQL Database
  # postgres:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_DB: myapp_development
  #     POSTGRES_USER: developer
  #     POSTGRES_PASSWORD: password
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

  # Redis Cache
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"

  # MongoDB
  # mongo:
  #   image: mongo:6
  #   environment:
  #     MONGO_INITDB_ROOT_USERNAME: admin
  #     MONGO_INITDB_ROOT_PASSWORD: password
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongo_data:/data/db

  # Mailcatcher (for testing emails)
  # mailcatcher:
  #   image: schickling/mailcatcher
  #   ports:
  #     - "1080:1080"  # Web interface
  #     - "1025:1025"  # SMTP server

# volumes:
#   postgres_data:
#   mongo_data: 